<!DOCTYPE html>
<html>
<head>
  <title>List of all Users</title>
</head>
<body>
  <section class="flex-col mx-auto">
    <h2 class="page-title">List of all Users</h2>

    <% @users.each do |user| %>
      <article class="flex gap-6 mb-6">
        <%= image_tag user.photo, alt: user.name %>
        <div class="flex m-2 px-4 py-2 w-[80%] lg:w-[25vw] border-2 border-black justify-between">
          <h3 class="text-2xl font-bold self-center"><%= user.name %></h3>
          <p class="text-sm self-end">Number of posts: <%= user.posts_counter %></p>
        </div>
      </article>
    <% end %>
  </section>
</body>
</html>
