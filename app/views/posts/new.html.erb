<h2>Create a new Post</h2>

<div>
  <%= simple_form_for @post, url: user_posts_path(@user) do |f| %>
    <%= f.input :title, :placeholder => "Post Title", required: true %>
    <%= f.input :text, :placeholder => "Post Title", required: true %>
    <%= f.button :submit %>
  <% end %>
</div>