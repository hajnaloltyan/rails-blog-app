<h2>Add comment</h2>

<div>
  <%= simple_form_for @comment, url: user_post_comments_path(@user.id, @post.id) do |f| %>
    <%= f.input :text, :placeholder => "Comment", required: true %>
    <%= f.button :submit %>
  <% end %>
</div>